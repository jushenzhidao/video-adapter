# --- 运行时 ---
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7

# --- 服务进程 ---
# gunicorn 负责进程管理（worker 回收 / 优雅排空 / 心跳），uvicorn-worker 提供 asyncio worker。
# ⚠️ worker_class 不要写 `uvicorn.workers.UvicornWorker`：uvicorn 0.52 起该模块已弃用
#    （实测会打印 DeprecationWarning），官方指定改用 uvicorn-worker 包。详见 gunicorn.conf.py。
gunicorn>=23.0
uvicorn-worker>=0.3

# --- 上报（可观测）---
# 未配置 LOGFIRE_TOKEN 时只在本地收集，不外发；没装这个包也能起（装配失败只降级）。
# ⚠️ 不引入 opentelemetry-instrumentation-* ：自动埋点的属性集不由我们控制，
#    本项目的上报字段表是契约，自己写（见 adapter/observability.py）。
logfire>=5.0

# --- 共享状态后端（任务表 + 限流桶，两者都用它）---
# sqlite 与进程内限流桶都走标准库，无需依赖。
# 需要它的两种情况：
#   ① 多实例部署任务表       TASK_STORE=redis
#   ② 跨进程共享限流桶       RATE_LIMIT_STORE=redis
#     （不开限流共享桶时，实际配额会按进程数放大 —— 见 docs/03_引擎架构.md §7.1）
# ⚠️ 配了 redis 却没装包 ⇒ **启动直接报错**，不静默降级（静默降级会让配额按进程数放大）。
# redis>=5.0
