.venv/
__pycache__/
*.pyc
*.pyo

# --- 运行产物 ---------------------------------------------------------------
.tasks.sqlite3
.tasks.sqlite3-wal
.tasks.sqlite3-shm
.media/
*.log

# 部署验证报告：含测试机公网 IP、真实上游 task id 与运行期凭据处置记录，
# **本仓库是 public** ⇒ 与 .media/ 同理，属运行产物，不入库。
reports/

# --- 环境文件（模板除外）----------------------------------------------------
.env
.env.*
!.env.example

# --- 内部工作记录（记忆 / 踩坑台账 / 决策草稿）------------------------------
# 这里放的是排障过程与内部判断（含真实上游任务 id 等），**不对外发布**。
.workbuddy/

# --- 工具缓存 ---------------------------------------------------------------
.pytest_cache/
.ruff_cache/
.mypy_cache/
